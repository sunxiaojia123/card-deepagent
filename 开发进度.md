# 开发进度

> 基于「任务拆分.md」的完成记录，含额外创建的功能清单。

---

## Phase 1：基础对话 ✅ 全部完成

### 任务 1.1 — 项目脚手架搭建 ✅

- 目录结构：`src/`、`src/middleware/`、`src/tools/`、`src/schemas/`、`src/mcp/`、`tests/`、`skills/base/`、`config/`
- `pyproject.toml`：依赖 deepagents(0.6.12, PyPI)、langgraph、fastapi、sse-starlette、psycopg、langchain-deepseek
- `config/settings.py`：配置中心，模型 `deepseek:deepseek-v4-pro`，python-dotenv 自动加载 `.env`
- 项目独立 `.venv`（Python 3.12.13）

### 任务 1.2 — Postgres Checkpointer ✅

- Docker 容器 `trading-pg`（postgres:16-alpine）
- `src/checkpointer.py`：`checkpointer_session()` 异步上下文管理器，封装 `AsyncPostgresSaver`
- PG 自动建表：`checkpoints`、`checkpoint_blobs`、`checkpoint_writes`、`checkpoint_migrations`
- 4 项测试通过

### 任务 1.3 — TradingContext 定义 ✅

- `src/context.py`：`TradingContext(TypedDict, total=False)`（5 字段）+ `build_context()` 工具函数
- 5 项测试通过

### 任务 1.4 — 基础 Agent + System Prompt ✅

- `src/agent.py`：`TRADING_ORCHESTRATOR_PROMPT` + `build_agent()` 工厂函数
- 5 项测试通过（含单轮/多轮 live 测试）

### 任务 1.5 — SSE 流式适配器 ✅

- `src/stream.py`：`sse_adapter()` 将 agent stream 转为 `{event, data}` + `sse_events_to_str()` 转 SSE 文本
- 协议：`event=text|done|error`
- 4 项测试通过

### 任务 1.6 — FastAPI 对话接口 ✅

- `src/api.py`：FastAPI app，lifespan 管理 checkpointer + agent 生命周期
- `POST /conversations/{id}/chat/stream`：SSE 流式端点
- Auth：`Authorization: Bearer <user-id>` mock JWT
- 5 项测试通过

### 任务 1.7 — 会话 CRUD ✅

- `src/db.py`：`conversations` 表管理（autocommit），`create_conversation()` / `list_conversations()`
- 新增端点：
  - `POST /conversations` — 创建会话
  - `GET /conversations` — 列出用户会话
  - `GET /conversations/{id}/history` — 返回 `{messages: [{role, content}]}`，通过 `agent.aget_state()` 读取
- 5 项测试通过

### 任务 1.8 — 前端展示页面 ✅

- `static/index.html`：深色主题单页面聊天应用（纯 HTML/CSS/JS，无框架依赖）
- 用户切换（localStorage 持久化，`切/+/-` 按钮），切换自动重载会话列表
- 用户隔离：后端 `chat/stream` 和 `history` 端点增加 `conversation_exists()` 校验
- `start.sh`：一键启动脚本（自动启动 PG 容器 + uvicorn --reload）
- `GET /` 返回聊天页面，CORS 中间件

---

## 额外创建

### 开发环境

| 项目 | 说明 |
|------|------|
| `.env` | 密钥配置（`DEEPSEEK_API_KEY`），`config/settings.py` 通过 python-dotenv 自动加载 |
| `.gitignore` | 忽略 `.venv/`、`__pycache__/`、`.env`、`dist/` |
| `asgi-lifespan` | 测试中支持 FastAPI lifespan 事件 |

### PG 数据库

| 项目 | 值 |
|------|------|
| Host | `localhost` |
| Port | `5432` |
| Database | `trading_agent` |
| User | `trading` |
| Password | `trading123` |
| JDBC URL | `jdbc:postgresql://localhost:5432/trading_agent` |
| 容器名 | `trading-pg`（镜像 postgres:16-alpine） |

### 表结构

| 表 | 用途 |
|------|------|
| `checkpoints` | checkpoint 主记录 |
| `checkpoint_blobs` | 复杂数据序列化存储 |
| `checkpoint_writes` | 待处理写入队列 |
| `checkpoint_migrations` | schema 版本迁移记录 |
| `conversations` | 业务会话记录（conversation_id ↔ user_id 映射） |

### API 接口总览

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/health` | 健康检查 |
| POST | `/conversations` | 创建会话 |
| GET | `/conversations` | 列出用户会话 |
| POST | `/conversations/{id}/chat/stream` | SSE 流式对话 |
| GET | `/conversations/{id}/history` | 获取对话历史 |

---

## 踩坑记录

| 问题 | 解决 |
|------|------|
| Postgres checkpointer 的 `aget_tuple()` 无法加载 messages（blob 版本不匹配） | 改用 `agent.aget_state(config)` 读取历史 |
| psycopg 默认不 autocommit，`init_db()` CREATE TABLE 被回滚 | `conn.set_autocommit(True)` |
| `astream` 单 stream_mode 返回 2-tuple `(mode, data)`，非文档中的 3-tuple `(namespace, mode, data)` | 适配器按 `(mode, data)` 解析 |

---

## 测试概览

```
32 passed (Phase 1 全部), 0 failed
```

| 文件 | 测试数 | 说明 |
|------|--------|------|
| `test_agent.py` | 5 | Agent 构建 + 单轮/多轮对话 |
| `test_api.py` | 14 | API 端点 + SSE + 会话 CRUD + 用户隔离 |
| `test_checkpointer.py` | 4 | checkpointer 读写隔离 |
| `test_context.py` | 5 | TradingContext 定义与兼容性 |
| `test_stream.py` | 4 | SSE 适配器 |

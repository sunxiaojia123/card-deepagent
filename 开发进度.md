# 开发进度

> 基于「任务拆分.md」的完成记录，含额外创建的功能清单。

---

## Phase 1：基础对话（进行中）

### 任务 1.1 — 项目脚手架搭建 ✅

**完成内容：**

- 目录结构：`src/`、`src/middleware/`、`src/tools/`、`src/schemas/`、`src/mcp/`、`tests/`、`skills/base/`、`config/`
- `pyproject.toml`：依赖 deepagents(0.6.12, PyPI)、langgraph、fastapi、sse-starlette、psycopg、langchain-deepseek
- `config/settings.py`：配置中心，模型 `deepseek:deepseek-v4-pro`，全部支持环境变量覆盖
- 项目独立 `.venv`（Python 3.12.13）
- `.gitignore`：忽略 `.venv/`、`__pycache__/`、`.env` 等

**测试：** `from deepagents import create_deep_agent` 通过

---

### 任务 1.2 — Postgres Checkpointer ✅

**完成内容：**

- Docker 容器 `trading-pg`（postgres:16-alpine）
- `src/checkpointer.py`：`checkpointer_session()` 异步上下文管理器，封装 `AsyncPostgresSaver`
- PG 自动建表：`checkpoints`、`checkpoint_blobs`、`checkpoint_writes`、`checkpoint_migrations`

**测试（4 pass）：**

- `test_checkpoint_write_and_read` — 写入后按 checkpoint_id 精确读回
- `test_different_thread_isolation` — 不同 thread_id 互不干扰
- `test_multi_turn_same_thread` — 同 thread 多次写入
- `test_unused_thread_returns_none` — 不存在的 thread 返回 None

---

### 任务 1.3 — TradingContext 定义 ✅

**完成内容：**

- `src/context.py`：
  - `TradingContext(TypedDict, total=False)`：`user_id`、`conversation_id`、`enabled_skills`、`mcp_server_ids`、`tenant_id`
  - `build_context()` — None 值不写入 dict

**测试（5 pass）：**

- `test_trading_context_fields` — 5 个字段完整
- `test_build_context_minimal` — 最少参数
- `test_build_context_full` — 全参数
- `test_build_context_optional_none_not_set` — None 值不污染
- `test_context_schema_compatible` — 可传入 `create_deep_agent`

---

### 任务 1.4 — 基础 Agent + System Prompt ✅

**完成内容：**

- `src/agent.py`：
  - `TRADING_ORCHESTRATOR_PROMPT`：角色定义、话术边界、合规要求
  - `build_agent(model, system_prompt, checkpointer)` 工厂函数

**测试（5 pass）：**

- `test_system_prompt_not_empty` — prompt 含关键信息
- `test_build_agent_without_checkpointer` — 无 checkpointer 构建
- `test_build_agent_with_checkpointer` — 传入 checkpointer 构建
- `test_agent_single_turn_invoke` — 单轮对话返回 AIMessage
- `test_agent_multi_turn_with_checkpointer` — 多轮上下文保持

---

## 额外创建（超出任务计划的产物）

### 开发环境

| 项目 | 说明 |
|------|------|
| `.env` | 密钥配置（`DEEPSEEK_API_KEY`），`config/settings.py` 通过 python-dotenv 自动加载 |
| `.gitignore` | 忽略 `.venv/`、`__pycache__/`、`.env`、`dist/` |

### PG 数据库连接信息

| 项目 | 值 |
|------|------|
| Host | `localhost` |
| Port | `5432` |
| Database | `trading_agent` |
| User | `trading` |
| Password | `trading123` |
| JDBC URL | `jdbc:postgresql://localhost:5432/trading_agent` |
| 容器名 | `trading-pg`（镜像 postgres:16-alpine） |

### 表结构

| 表 | 用途 |
|------|------|
| `checkpoints` | checkpoint 主记录（PK: thread_id, checkpoint_ns, checkpoint_id） |
| `checkpoint_blobs` | 复杂数据序列化存储（PK: thread_id, checkpoint_ns, channel, version） |
| `checkpoint_writes` | 待处理写入队列 |
| `checkpoint_migrations` | schema 版本迁移记录 |

---

## 测试概览

```
14 passed (tasks 1.2–1.4), 0 failed
```

| 文件 | 测试数 | 说明 |
|------|--------|------|
| `test_checkpointer.py` | 4 | checkpointer 读写隔离 |
| `test_context.py` | 5 | TradingContext 定义与兼容性 |
| `test_agent.py` | 5 | Agent 构建 + 单轮/多轮对话 |
